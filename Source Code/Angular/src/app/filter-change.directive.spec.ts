import { FilterChangeDirective } from './filter-change.directive';

describe('FilterChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterChangeDirective();
    expect(directive).toBeTruthy();
  });
});
